{% extends 'base.html.twig' %}

{% block title %}Nomproduit{% endblock %}

{% block body %}
    <h1>Nomproduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ nomproduit.id }}</td>
            </tr>
            <tr>
                <th>Nomp</th>
                <td>{{ nomproduit.nomp }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ nomproduit.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_nomproduit2_index') }}">back to list</a>

    <a href="{{ path('app_nomproduit2_edit', {'id': nomproduit.id}) }}">edit</a>

    {{ include('nomproduit2/_delete_form.html.twig') }}
{% endblock %}

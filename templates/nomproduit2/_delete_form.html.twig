<form method="post" action="{{ path('app_nomproduit2_delete', {'id': nomproduit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nomproduit.id) }}">
    <button class="btn">Delete</button>
</form>
